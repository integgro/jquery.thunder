@model Thunder.Data.IPaging<Jquery.Thunder.Models.Person>
@if (@Model.Count.Equals(0))
{
    <p>
        Nenhum registro localizado.</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>
                    Código
                </th>
                <th>
                    Nome
                </th>
                <th>
                    E-mail
                </th>
                <th>
                    Data de Nascimento
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var person in Model)
            {
                <tr>
                    <td>@person.Id
                    </td>
                    <td>@person.Name
                    </td>
                    <td>@person.Email
                    </td>
                    <td>@string.Format("{0:dd/MM/yyyy}", person.BirthDate)
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="4">
                    <a href="#" class="paged @(!Model.HasPreviousPage? "disabled" : "")" data-page="@(Model.HasPreviousPage ? Model.CurrentPage-1: -1)">&laquo;</a>
                    <select  class="paged">
                        @for (var i = 0; i < Model.PageCount; i++)
                        {
                            <option value="@i" @(Model.CurrentPage.Equals(i) ? "selected=selected" : "")>@string.Format("Página {0} de {1}", (i + 1), Model.PageCount)</option>   
                        }
                    </select>
                    <a href="#" class="paged @(!Model.HasNextPage? "disabled" : "")" data-page="@(Model.HasNextPage ? Model.CurrentPage+1: -1)">&raquo;</a>
                    <span class="paged-loading" style="display: none;">Carregando</span>
                </td>
            </tr>
        </tfoot>
    </table>
}
